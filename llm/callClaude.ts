export async function callClaude(prompt: string): Promise<string> {
  // Replace with real Claude API call using Anthropic SDK or HTTP fetch
  console.log("Calling Claude with prompt:", prompt.slice(0, 200), '...')
  return "// mock response from Claude\n"
}
